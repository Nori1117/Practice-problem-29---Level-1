#Given a list of numbers, write a python function to exchange the first
#n/2 elements of a list with the last n/2 elements. The function should return the new list.
#n represents the number of elements in the list. Assume that it will always be even.
